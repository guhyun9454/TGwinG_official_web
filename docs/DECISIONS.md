# T.G.winG 웹 — 기술·운영 결정

호스팅 종착지는 쿠러그(KU LUG) 자체 서버. 그래서 클라우드 락인을 피하는 "포터블" 제약이 곳곳에 붙는다.
✅ 채택 · 🔴 비추천.

---

## 1. 아키텍처 `확정`

기여자가 매학기 바뀌므로 세팅 단순함이 최우선.

| 옵션 | 스택/방식 | 장점 | 단점 |
|------|-----------|------|------|
| **단일 통합 ✅** | Next.js 하나(App Router + API Routes) | 세팅 1번, 진입 쉬움, 배포 단순 | 백엔드 아주 커지면 분리 필요할 수도 |
| 모노레포 | Turborepo에 web/api 폴더 | 추후 분리 여지 | 초기 복잡도↑ |
| 완전 분리 🔴 | Next + Spring/Nest 별도 | 백엔드 학습폭↑ | 세팅·배포·유지 2배, 무산 리스크↑ |

👉 **Next.js 단일 통합.** 분리는 진짜 필요해질 때. (옛 레포 React+Spring 분리안 폐기)

### 단일레포 ≠ 풀스택 강제 — 폴더 경계로 역할 분리

프론트만 하는 사람도 백엔드 안 건드리고 작업 가능. App Router가 원래 이 분리를 전제로 설계됨.

```
src/
├─ app/
│  ├─ (public)/          # 페이지 (프론트 주력)
│  └─ api/               # route handlers (백 담당 구역)
├─ components/           # 프론트 전용 (순수 React, 서버코드 import 금지)
│  ├─ ui/                # 디자인 시스템
│  └─ features/          # 도메인 UI
├─ lib/                  # 백 전용 (DB·인증·R2)
│  ├─ db/                # Drizzle 스키마·쿼리
│  ├─ auth.ts            # Auth.js 설정
│  └─ storage.ts         # R2 클라이언트
└─ types/                # 공유 계약 (프론트·백 둘 다 import)
```

- **담당 구역:** 프론트 = `app/` + `components/`, 백 = `lib/` + `app/api/`. 풀스택 한 명이 다 할 필요 없음.
- **연결 방식:** 백이 `lib/`에 함수 시그니처(빈 구현)만 먼저 박고 `types/`에 계약 정의 → 프론트는 그 타입만 보고 더미 데이터로 UI를 끝까지 완성. 내부 구현(DB 쿼리)은 몰라도 됨.
- **안전장치:** ESLint `import/no-restricted-paths`로 `components/` → `lib/db`·`lib/auth` import 차단 + 서버 모듈에 `import 'server-only'` → 클라이언트 번들에 새면 빌드 에러.

## 2. 프레임워크 `확정`

| 옵션 | 스택 | 장점 | 단점 |
|------|------|------|------|
| **Next.js ✅** | React 풀스택 | 자료 압도적·고용시장·SSR/SSG, 컨테이너화 쉬움 | React 러닝커브 |
| SvelteKit | Svelte | 가볍고 빠름, 코드 적음 | 생태계·자료 적음 |
| Astro | 콘텐츠 중심 | 아카이브·블로그형 최적, 초고속 | 동적 기능엔 부족 |

👉 **Next.js.** 자료량·생태계·이력서 가치.

## 3. DB `확정`

쿠러그 이전 위해 포터블해야 함.

| 옵션 | 스택 | 장점 | 단점 |
|------|------|------|------|
| **Postgres(Supabase로 시작) ✅** | 매니지드 Postgres + ORM | 무료 시작, SQL표준, 순수 Postgres라 포터블 | 무료는 7일 무접속 시 일시정지(Cron 핑으로 회피) |
| Neon | 서버리스 Postgres | 무료 넉넉, 일시정지 없음, 브랜칭 | 비교적 신생 |
| Supabase Auth/RLS까지 사용 🔴 | Supabase 풀기능 | 인증·실시간 따라옴 | 이전 시 락인 |
| 자가호스팅 Postgres | 쿠러그 서버 컨테이너 | 비용0, 종착지 | 학기 초까지 대기 |

👉 **순수 Postgres + ORM(Drizzle/Prisma).** Supabase는 DB만, Auth/RLS는 안 씀 → 이전 시 커넥션 문자열만 교체.

## 4. 파일 저장 (스터디 자료) `확정`

| 옵션 | 스택 | 장점 | 단점 |
|------|------|------|------|
| **Cloudflare R2 (S3 SDK) ✅** | `@aws-sdk/client-s3` | 10GB 무료·전송료 0, MinIO로 이전 쉬움 | 계정 1개 추가 |
| Supabase Storage 🔴 | Supabase | 한 곳 관리 | 무료 1GB뿐, 락인 |
| AWS S3 🔴 | S3 | 표준·무제한 | 전송료 과금, IAM 복잡 |
| MinIO(쿠러그 서버) | 자가호스팅 S3호환 | 비용0, 종착지 | 학기 초 대기 |

👉 **R2 + S3 호환 SDK.** S3 규격으로 짜두면 R2 → 쿠러그 MinIO를 env 교체로 이전.

## 5. 인증 (로그인 + 멤버 판정) `확정`

| 옵션 | 스택 | 장점 | 단점 |
|------|------|------|------|
| **Auth.js + GitHub OAuth + Org 체크 ✅** | NextAuth | 무료·포터블, Org 멤버=자동 멤버 | Org 초대 관리 필요 |
| Supabase Auth 🔴 | Supabase | 세팅 쉬움 | 락인 |
| Clerk | SaaS | UI 완성도 | 무료한도 후 유료, 외부 의존 |
| Lucia | 자체 구현 | 완전 제어·포터블 | 직접 짤 게 많음 |
| 학번·이름 DB 대조 🔴 | 자체 | 회원가입 없이 간단 | 학번·이름은 공개정보 → 사칭 가능(인증 무의미) |

👉 **Auth.js + GitHub OAuth, 멤버 판정 = GitHub Org 멤버십.** 승인 테이블 없이 Org 초대로 끝. (옛 레포 "학번·이름 대조"는 보안상 폐기)

## 6. 호스팅 `결정필요`

| 옵션 | 스택 | 장점 | 단점 |
|------|------|------|------|
| **종착지: 쿠러그 서버 ✅** | 자가호스팅(Docker) | 비용0, 관리=쿠러그 본업이라 인계 최선 | 학기 초 진입, 공유서버 사양 변동 |
| **개발 단계: Vercel ✅** | 매니지드 | 즉시 시작·미리보기 배포 | Hobby는 비상업 한정 |
| 교내 서버 자가관리 🔴 | tgwing 직접 | 비용0 | 학생 관리 → 졸업하면 방치(무산 패턴) |
| Cloudflare Pages | 매니지드 | 대역폭 무제한 | 풀스택 기능 일부 제약 |

👉 **Vercel로 개발 → 학기 초 쿠러그 이전.** 포터블 규칙(3·4·5) 지키면 이전 = `docker compose up` + env 교체.

## 7. 도메인 `결정필요`

| 옵션 | 상태 / 등록처 | DNS 관리처 | 장점 | 단점 |
|------|--------------|-----------|------|------|
| tgwing.kr (기존 보유) | **살아있음.** 가비아 등록, 등록인 **개인(안병훈)**, ~2032 만료, 현재 AWS 서울으로 연결 | 가비아 (ns1/ns2.hosting.co.kr) | 짧은 공식 .kr, 이미 보유·장기 결제됨 | 개인 명의 → 동아리 공용계정으로 소유권 이전/위임 필요(인계 리스크) |
| tgwing.khu.ac.kr | 정보처 신청 | 정보처 | 무료·학교 소유(인계 자동) | 행정 대기, 외부 DNS 허용 확인 필요 |
| tgwing.dpdns.org | DigitalPlat FreeDomain(무료, GitHub PR로 신청) | DigitalPlat(Cloudflare 기반) 포털/PR로 레코드 설정 | 완전 무료·즉시 | 제3자 무료 서비스 의존(중단 리스크), 브랜드 약함 |
| tgwing.khlug.org | 쿠러그 서브도메인 | 쿠러그 (whois.com NS) | 무료, 기존에 쓰던 방식 | 쿠러그 종속 표기 |
| 신규 구매 | Cloudflare 등 | 구매처/Cloudflare | 독립 브랜드 자유 | 연 ~1.5만원, 매년 갱신 책임 |

👉 결정 전 **tgwing.kr 소유권부터 확인**(안병훈 님 연락 → 공용계정으로 이전/위임 가능한지). 넘겨받으면 짧은 .kr를 이미 보유한 셈이라 유력. 안 되면 khu.ac.kr / dpdns.org / khlug.org / 신규구매 중 택. (참고: FreeDomain — https://github.com/DigitalPlatDev/FreeDomain )

## 8. 자산 소유 `확정`

| 옵션 | 방식 | 장점 | 단점 |
|------|------|------|------|
| **동아리 공용계정 ✅** | 공용 Google/GitHub | 개인 졸업과 무관 | 비번 공유 관리 필요 |
| 개인(회장) 계정 🔴 | — | 당장 편함 | 졸업=계정 증발=무산 1순위 원인 |
| 학과/학교 귀속 | 행정 등록 | 가장 안정 | 행정 협의 부담 |

👉 **공용계정.** 단 비번을 한 사람만 알면 무의미 → 9번 안전장치와 세트.

## 9. 인수인계 방식 `결정필요`

| 옵션 | 방식 | 장점 | 단점 |
|------|------|------|------|
| 담당자 지정 | 1~2명 | 단순 | 속인의존(졸업·잠수 시 위험) |
| **담당자 + 최소 안전장치 ✅** | Org Owner 2명+ / HANDOVER.md / 자동백업 | 거의 공짜로 속인의존 보완 | 초기 문서작업 약간 |
| 공유금고 풀세트 | 1Password 등 | 가장 견고 | 유료 도구·관리 부담 |

👉 **담당자 + 안전장치(12번 R1·R2·R3).** 셋 다 비용 0.

## 10. 연간 예산 상한 `결정필요`

| 옵션 | 연 비용 | 가능 | 리스크 |
|------|---------|------|--------|
| **사실상 ₩0 ✅** | ~1.5만(도메인) | Vercel Hobby+Supabase Free+R2 Free, 풀기능 | 무료 한계는 Cron·R2로 회피 |
| 연 ~10만 | ~10만 | 위 + 소액 초과분 | 매년 결제·정산 |
| 월 $25(Supabase Pro) 🔴 | ~36만 | 무중단·8GB DB·100GB | 고정지출, 카드 만료 시 정지 |

👉 **₩0 시작.** 쿠러그 이전 시 도메인값마저 0 가능. 무료 한계 닿으면 그때 Pro.

## 11. v1 범위 `결정필요`

| 옵션 | 포함 | 기간 | 비고 |
|------|------|------|------|
| **Public 전체 ✅** | 홈+프로젝트 아카이브+블로그 아카이브+로그인 | 4~5주 | 외부에 보이는 성과 |
| 랜딩만 | 홈 1페이지 | 1~2주 | 가장 빠른 동기부여 |
| Public + 스터디 자료 | 위 + 스터디 자료 1개 Private | 6~7주 | 내부 가치까지 |
| 현 PRD 4단계 전부 🔴 | 풀스코프 | 8~12주 | 무산 리스크↑ |

👉 **Public 전체 = v1.** 스터디 자료는 다음 기수.

## 12. 오픈소스 · 라이선스 · 안전장치

| 라이선스 | 성격 | 장점 | 단점 |
|----------|------|------|------|
| **MIT ✅** | 관대 | 가장 흔함·기여 장벽 낮음, 출처 표기 | 재사용 제약 거의 없음 |
| Apache 2.0 | 관대+특허조항 | 특허 보호 | 약간 복잡 |
| AGPL | 강한 카피레프트 | 변형도 공개 강제 | 기여·재사용 장벽↑ |

> 공개 레포 자체는 무료 — 비용은 트래픽·용량이 정하지 레포 공개 여부와 무관. 비밀키는 커밋 금지, `.env.example`만.
> ⚠️ 현재 레포 private. 공개(오픈소스) 여부는 선행 결정 — public 안 가면 MIT/오픈소스 항목 보류. (ROADMAP Phase 0)

| 안전장치 | 내용 | 비용 | 효과 |
|----------|------|------|------|
| **R1 ✅** | GitHub Org Owner 항상 2명+ | 0 | 1명 졸업해도 권한 유지 |
| **R2 ✅** | HANDOVER.md(계정·도메인·결제일·비번위치) | 0 | 속인의존 보완 |
| **R3 ✅** | DB 주1회 백업 + R2 버전관리 | 0 | 재생성 불가 자산 보호 |
| R4 | Vercel Cron 주간 핑 | 0 | 무료 DB 일시정지 회피(클라우드 단계만) |

👉 **MIT + R1·R2·R3.**

## 13. 쿠러그 협업 — tgwing 기여 범위 `결정필요`

쿠러그는 "다른 동아리도 쓸 공용서버 확장성"을 고민 중. 선을 그어야 본업이 안 무너짐.

| 구분 | 내용 | 판단 |
|------|------|------|
| ✅ 할 것 | 자기 앱을 Docker 컨테이너로 패키징 | 자기 일, 이전에도 필요 |
| ✅ 할 것 | 1호 입주자 버그 리포팅 + 배포/접속 가이드 문서 | 저비용·고환영 |
| 🔴 안 할 것 | reverse proxy·quota·k8s 멀티테넌트 플랫폼 대신 구축 | 남의 인프라 떠맡음=본업 무산. 쿠러그 현업 개발자 몫 |

👉 **Docker 패키징 + 문서까지만.**

## 14. 브랜치 전략 & 보호 `결정필요`

| 옵션 | 방식 | 장점 | 단점 |
|------|------|------|------|
| **GitHub Flow ✅** | main + feature/* | 단순, PR마다 Vercel 프리뷰=스테이징 | prod 전 통합 버퍼 없음 |
| main + dev | feature→dev→main | dev를 Vercel 상시 배포=스테이징+failover 대기소 | 단계 하나 더 |
| Git Flow 🔴 | main+develop+release+hotfix | 버전 릴리스에 적합 | 동아리엔 과함 |

👉 **GitHub Flow(main only).** 통합 버퍼 원하면 dev 추가하고 Vercel 상시 배포로 묶기.

보호 규칙: **무료 private이라 지금은 못 켬.** GitHub Education Team(추천) 또는 public 전환 후 적용 →
main 직접 push 금지 / PR 필수 / 승인 ≥1 / CI(lint·build) 통과 / squash·linear / force-push·삭제 금지.

## 15. 가용성 / failover `결정필요`

쿠러그 다운 시 대응. 핵심 함정: 앱은 어디든 띄우지만 **DB·파일이 쿠러그 안에 있으면 넘겨도 빈 껍데기.**

| 옵션 | 방식 | 장점 | 단점 |
|------|------|------|------|
| **(A) 수동 failover ✅** | 포터블 + Vercel 웜 스탠바이, 다운 시 DNS 플립 | 거의 공짜, 단순 | 복구에 몇 분, 수동 |
| (B) 즉시 failover | compute만 쿠러그, DB(Supabase/Neon)+파일(R2)은 매니지드 유지 | DNS 플립으로 즉시, 백업 외부 보관 | 완전 자가호스팅 포기, 소액 비용·외부 의존 |
| (C) 완전 자가호스팅 | DB·파일까지 쿠러그 | 비용0·독립 최고 | failover 사실상 불가 |
| 자동 failover 🔴 | health-check + DNS LB + 데이터 복제 | 무중단 | 유지보수 지옥=무산 원인 |

👉 **(A) 수동 failover 기본.** 가용성이 더 중요하면 (B). 자동 failover는 만들지 않는다. 쿠러그는 전원복구 자동부팅+원격리부팅+전담운영이라 다운 자체가 드묾.

## 16. 담당자 배정 `결정필요`

- GitHub Org Owner (2명+): ____ , ____
- 공용계정·비번 관리: ____
- 도메인 결제·연장(또는 쿠러그/정보처 문의): ____
- README / 문서화: ____
- 이슈·PR 템플릿: ____
- 디자인 시스템(색·폰트·로고): ____
- 쿠러그 창구: ____

---

## 회의에서 정할 것 (8개)
호스팅 · 도메인 · 인수인계 방식 · 예산 상한 · v1 범위 · 쿠러그 협업 범위 · 브랜치 전략(+공개여부) · failover
